# Python
env/
venv/
__pycache__/
*.py[cod]
.python-version

# Data & Notebooks
.ipynb_checkpoints
*.pkl
# Feature index pkls are needed to use the committed matrices — keep them
!data/features/album_ids.pkl
!data/features/artist_ids.pkl
# Trained model artefacts are large and regenerable — not versioned
data/model/
data/model_v2/
data/model_v3/

# v1 feature matrices — superseded by album_genre_matrix and album_record_label_matrix
data/features/album_tags_matrix.npz
data/features/album_labels_matrix.npz
data/features/album_types_matrix.npz

# Secrets
.env
.venv

.DS_Store

# Ignore local AI environment artifacts
.claude/
data/model*/*.joblib
