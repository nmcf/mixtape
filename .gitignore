# Python
env/
venv/
__pycache__/
*.py[cod]
.python-version

# Data & Notebooks
.ipynb_checkpoints
*.pkl
# album_ids.pkl is needed by the app at runtime — keep it tracked
!data/index/album_ids.pkl
# Trained model artefacts are large and regenerable — not versioned
development/data/model/
development/data/model_v2/
development/data/model_v3/
development/data/model_v4/

# v1 feature matrices — superseded by album_genre_matrix and album_record_label_matrix
development/data/features/album_tags_matrix.npz
development/data/features/album_labels_matrix.npz
development/data/features/album_types_matrix.npz

# Local MusicBrainz cache — large (~90 MB), regenerable, never part of the release
development/musicbrainz.duckdb

# Secrets
.env
.venv

.DS_Store

# Ignore local AI environment artifacts
.claude/
development/data/model*/*.joblib
__pycache__/
*.pyc
