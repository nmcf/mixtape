# AI Context — mixtape

Internal reference for Claude Code sessions. Updated as the project evolves.

## What this project is

Album recommendation engine trained on the MusicBrainz open music database. A user enters an artist name, selects an album, and receives 10 similar album recommendations via a KNN model. Three model versions exist and can be compared side-by-side in the app.

## Tech stack

- **Data store:** MusicBrainz PostgreSQL (source) → Parquet files (working copy) → DuckDB (query layer for feature extraction)
- **ML:** scikit-learn `NearestNeighbors` with cosine distance, sparse scipy matrices
- **App:** Streamlit (+ `streamlit-searchbox` for the artist typeahead; custom HTML/SVG components for knobs & faders)
- **Environment:** Python 3.11, virtualenv in `env/`

## Project structure

Folders map 1-to-1 to the five pipeline steps described in the README "How it works" section.

```
mixtape/
├── 1-data/                             # Step 1 — Data import
│   ├── 01-postgres-to-parquet.ipynb    # Imports Parquet tables from Postgres via DuckDB
│   ├── 02-parquet-to-dataframes.ipynb  # Merges Parquet into pickled DataFrames
│   ├── 03-feature-country-import.ipynb # DuckDB SQL → sql_feature_artist_country_fast.parquet (needs Postgres)
│   ├── 04-feature-track-stats-import.ipynb # DuckDB SQL → sql_feature_album_track_stats.parquet (needs Postgres)
│   ├── queries/                        # SQL files for DuckDB Postgres extraction
│   │   ├── mb_artist_country_fast_duckdb_release.sql  # 10-signal country resolution chain
│   │   ├── mb_album_stats_duckdb.sql                  # Album track statistics
│   │   ├── mb_album_live_flag_duckdb.sql              # Live secondary-type flag export
│   │   └── mb_album_compilation_flag_duckdb.sql       # Compilation secondary-type flag export
│   ├── schema-diagrams.md              # Parquet table relationships and pipeline data flow
│   └── SchemaSpy/                      # Auto-generated HTML reference for MusicBrainz Postgres schema
├── 2-eda/                              # Step 2 — Exploratory data analysis
│   ├── 01-EDA-albums.ipynb
│   ├── 02-EDA-artists.ipynb
│   ├── 04-EDA-year.ipynb               # Year/era source coverage, agreement, outliers
│   └── 05-EDA-tags-labels.ipynb        # Tag/label coverage, genre blend, label allowlist analysis
├── 3-features/                         # Step 3 — Feature engineering
│   ├── 01-album-artist-index.ipynb     # Master album/artist ID index (run first; album_ids/artist_ids.pkl)
│   ├── 02-feature-genre.ipynb          # album_tags, artist_tags, album_genre matrices (3-tier blend)
│   ├── 03-feature-label.ipynb          # album_labels, album_types, album_record_label matrices
│   ├── 04-feature-ratings.ipynb        # Bayesian-weighted rating vectors
│   ├── 05-feature-country.ipynb        # Builds album_country_matrix.npz
│   ├── 06-feature-track-stats.ipynb    # Builds album_track_stats_matrix.npz
│   ├── 08-feature-assembly.ipynb       # Inspect combined matrix (display only)
│   ├── 14-feature-temporal.ipynb       # Builds album_era.parquet, album_era_matrix.npz, album_temporal_matrix.npz
│   └── feature_charts/                 # Saved feature diagnostic plots + generate_feature_charts.py
├── 4-model/                            # Step 4 — Model training
│   ├── 01-knn-v1-training.ipynb        # Trains v1 KNN, saves to data/model/
│   ├── 02-knn-query.ipynb              # KNN query prototyping (display only)
│   ├── 03-knn-v2-training.ipynb        # Trains v2 KNN, saves to data/model_v2/
│   └── 04-knn-v3-training.ipynb        # Trains v3 KNN, saves to data/model_v3/
├── 5-app/                              # Step 5 — Streamlit app
│   ├── app.py                          # Streamlit app — v1 only
│   ├── app_v2.py                       # Streamlit app — v1 vs v2 side-by-side
│   ├── app_v3.py                       # Streamlit app — v1 vs v2 vs v3 side-by-side
│   ├── app_v3_weighted.py              # Streamlit app — v3 features with runtime knobs + filters (current)
│   └── knob_component/                 # Custom HTML/SVG sidebar widgets (served on :8502)
│       ├── index.html                  #   rotary knob panel (feature weights)
│       └── switch.html                 #   vertical fader switch (album-type filters)
├── archive/                            # Superseded files — pending deletion (see Archive section below)
├── data/
│   ├── mb_*.parquet                    # MusicBrainz exports (committed)
│   ├── sql_feature_*.parquet           # Feature-specific exports (country, track stats)
│   ├── pickles/                        # Intermediate DataFrames (gitignored)
│   ├── features/                       # Sparse feature matrices + album_ids/artist_ids.pkl (committed)
│   ├── model/                          # v1 model artefacts (gitignored)
│   ├── model_v2/                       # v2 model artefacts (gitignored)
│   └── model_v3/                       # v3 model artefacts (gitignored)
├── docs/
├── Planning/                           # Planning documents for restructuring sessions
├── musicbrainz.duckdb
├── requirements.txt
└── README.md
```

## Archive

Files moved to `archive/` and no longer part of the active pipeline.

| File | Why archived |
|------|-------------|
| `01-feature-tags-labels.ipynb` | Split into `01-album-artist-index.ipynb`, `02-feature-genre.ipynb`, and `03-feature-label.ipynb` |
| `10-feature-genre-tags.ipynb` | Replaced by `02-feature-genre.ipynb` |
| `03-EDA-master.ipynb` | `master_df` is a 157M-row cartesian product with no downstream consumers; removed |
| `year_feature.md` | Planning notes for era feature; superseded by `Planning/feature-year.md` and the notebook |
| `cleanup.md` | Change log from 2026-06-02; superseded by git history |
| `07-feature-era.ipynb` | Consolidated into `14-feature-temporal.ipynb` (era + year in one notebook) |
| `10-feature-year.ipynb` | Consolidated into `14-feature-temporal.ipynb`; used a different data source and was never in app |
| `05-tune-temporal-ratio.ipynb` | Tuning notebook — blocked, `lastfm_album_similarity.parquet` does not exist; YEAR_WEIGHT set analytically instead |
| `album_year_col.npz` | Intermediate artifact from tuning experiments; not produced by the combined temporal notebook |
| `album_tags_matrix.npz` | v1 album-only tag matrix; superseded by `album_genre_matrix.npz` (3-tier blend) |
| `album_labels_matrix.npz` | v1 record label matrix; superseded by `album_record_label_matrix.npz` |
| `album_types_matrix.npz` | v1 label-type matrix; superseded by `album_record_label_matrix.npz` |

## Album universe

All matrices are built at the full album universe of **1,758,488** release groups (the
`valid_albums` scope — studio + official live + single-artist best-of). This replaced the
earlier 2,241,402 "type=1 only" universe that included live bootlegs, remixes and comps.

## Model versions

Numbers below are after the album-scope rebuild (universe = 1,758,488).

| Version | Feature blocks | Tag columns | Features (post-prune) | Albums indexed | Notebook |
|---------|---------------|-------------|----------------------|----------------|----------|
| v1 | tags · labels · types · ratings | 2,684 | 5,653 | 1,070,021 | 04-knn-training.ipynb |
| v2 | v1 + country · track stats | 2,684 | 7,679 | 1,758,005 | 11-knn-v2-training.ipynb |
| v3 | genre tags · record_label · ratings · country · track stats | 10,255 | 15,247 | 1,758,047 | 12-knn-v3-training.ipynb |

The current app, **`app_v3_weighted.py`**, uses the v3 feature blocks but does **not** load a
trained model — it reads the five raw feature matrices and computes weighted cosine directly
(see below), so the runtime knobs can reweight features. The trained models (v1/v2/v3) are still
produced by their notebooks but are gitignored.

Note: v1/v2 use the separate `album_labels_matrix` (2,958 cols) + `album_types_matrix` (10 cols);
v3 uses the combined `album_record_label_matrix` (2,968 cols). v2/v3 index ~1.76M albums vs v1's
1.07M (v1's tag-only feature set leaves more albums with no signal).

## Weighted app (Option B) — `app_v3_weighted.py`

Exposes six feature blocks as sidebar **knobs** (a custom HTML/SVG component): genre, ratings,
record_label, track_stats, country, era. Loads the raw (unweighted) matrices and precomputes
per-block per-album sum-of-squares once at startup, then per query computes:
`numerator = Σ wᵦ²·(Xᵦ·qᵦ)`, album norms `= sqrt(Σ wᵦ²·ssqᵦ)`. No full-matrix rebuild or
renormalisation per query (~60–180 ms over the index). The album dropdown is filtered to albums
that have signal under the *current* weights, so it never offers an unservable album.

**Custom sidebar widgets** live in `5-app/knob_component/` and are both served over one tiny
background `HTTPServer` thread on port 8502 (Streamlit's built-in component file server failed in
this setup), registered via `declare_component(url=...)`. All postMessages to Streamlit must carry
`isStreamlitMessage: true`.

- **Knob panel** (`index.html`): a guitar-amp-style rotary knob panel, 0–11 per knob, mapped to
  block weight via `dial_to_weight(d) = d/11·2.0` (0 → off, 11 → 2.0 max). Click a tick around the
  ring to set a value. Defaults (dial units): Country = 2, Era = 4 (moderate soft signal), others = 6
  (mirrors the v3 training weights, country downweighted). `BLOCK_LABELS` carries `<br>` for two-line
  knob labels — strip it
  (`.replace('<br>',' ')`) anywhere the label is shown outside the component (e.g. the EQ caption).
- **Blade/fader switches** (`switch.html`): two mixing-console-style **vertical faders**, placed
  side by side via `st.columns(2)`. Three detents with option labels at top / middle / bottom;
  click a label or anywhere along the track to slide the cap. One declared component, instantiated
  twice with different keys (`live_switch`, `hits_switch`); the Python wrapper is `blade_switch()`.

**State model (important).** Each widget is the **single source of truth** for its own value —
there is no `session_state` mirror. Streamlit persists a keyed component's last emitted value, and
the knob panel is *seeded* each rerun from that persisted value (`st.session_state["knob_panel"]`)
so a silent iframe **remount** restores the user's dials instead of snapping to defaults. After
first render each iframe **ignores ordinary inbound renders** (`initialised` flag) — only a change
in the knob's `reset_nonce` arg (flipped by the "Preset" button's `on_click`) makes it
re-apply, using each knob's `defaultValue` and re-emitting so Python's `result` stays in sync. The
switches have no external reset, so they ignore all inbound renders after init. This event-based
scheme replaced an earlier fragile 600 ms time-window guard that caused cross-widget glitches and a
stale-value "memory" bug.

**Two album-type filters** (the fader switches above), each an exact MusicBrainz secondary-type
lookup via `filter_by_flag()` / `load_flag_ids()`:
- **Live Albums** — Live / Both / Studio (secondary type Live=6, `mb_album_live_flag.parquet`)
- **Greatest Hits** — Hits / Both / Albums (secondary type Compilation=1,
  `mb_album_compilation_flag.parquet`)

Both default to Both and chain on both the artist-album dropdown and the recommendation results.
Replaced an earlier album-name keyword heuristic that mis-classified titles with no "live"
keyword (e.g. "Set List", date-format concert titles). See [[reference-mb-release-types]].

**Artist search** is a live typeahead (`streamlit-searchbox`'s `st_searchbox`): as you type, a
dropdown of matching artists from the lookup appears and refines per keystroke (150 ms debounce,
prefix matches first, capped at 50). `artist_index()` caches the ~566k unique names with a
precomputed lowercased column so the per-keystroke substring scan is fast; `make_artist_search()`
builds the callback. Selecting a suggestion goes straight to the album picker — this replaced the
old `text_input` + separate "Pick the Artist" selectbox.

UI copy: title "Mixtape — Dial in Your Sound"; sidebar header "Tune your sound" / caption "Set the
balance to find your sound"; flow Set the Tone (type an artist) → Pick a Starting Album →
Checkout these albums; results caption "EQ — …".

Recommendations skip Various-Artists releases (null `artist_name`, ~1.5% of the index — VA
samplers that passed the import filter via the studio branch), since a release with no single
artist isn't actionable. Seeds are unaffected (dropdown is built from a selected real artist).

## Pipeline stages (run in order)

### 1-data
1. `1-data/01-postgres-to-parquet.ipynb` — connects to MusicBrainz Postgres via DuckDB, exports tables to parquet
2. `1-data/02-parquet-to-dataframes.ipynb` — joins parquets into pickled DataFrames
3. `1-data/03-feature-country-import.ipynb` — DuckDB SQL → `sql_feature_artist_country_fast.parquet` (needs Postgres)
4. `1-data/04-feature-track-stats-import.ipynb` — DuckDB SQL → `sql_feature_album_track_stats.parquet` (needs Postgres)

### 2-eda
5. `2-eda/01–05` — EDA reports (display only)

### 3-features
6. `3-features/01-album-artist-index.ipynb` — builds the master `album_ids.pkl` / `artist_ids.pkl` row index (run first; every feature notebook loads it)
7. `3-features/02-feature-genre.ipynb` — builds `album_tags`, `artist_tags`, and the blended `album_genre` matrices (3-tier blend; see design decisions)
8. `3-features/03-feature-label.ipynb` — builds `album_labels`, `album_types`, and combined `album_record_label` matrices
9. `3-features/04-feature-ratings.ipynb` — computes Bayesian-weighted rating vectors
10. `3-features/05-feature-country.ipynb` — `album_country_matrix.npz`
11. `3-features/06-feature-track-stats.ipynb` — `album_track_stats_matrix.npz`
12. `3-features/14-feature-temporal.ipynb` — `album_era.parquet`, `album_era_matrix.npz`, `album_temporal_matrix.npz`, `temporal_year_scaler.json`
13. `3-features/08-feature-assembly.ipynb` — inspect and validate (display only)

### 4-model
14. `4-model/01-knn-v1-training.ipynb` — assembles, prunes, normalises, trains, saves v1
15. `4-model/03-knn-v2-training.ipynb` — trains v2
16. `4-model/04-knn-v3-training.ipynb` — trains v3

## Key data files

| Path | Description |
|------|-------------|
| `data/mb_*.parquet` | Raw MusicBrainz exports |
| `data/sql_feature_artist_country_fast.parquet` | Artist country with 10-signal imputation (2.86M artists) |
| `data/sql_feature_album_track_stats.parquet` | Album track statistics (2.24M albums) |
| `data/mb_album_live_flag.parquet` | album_ids with MB Live secondary type (122k) — app Live/Studio filter |
| `data/mb_album_compilation_flag.parquet` | album_ids with MB Compilation secondary type (453k) — app Greatest Hits filter |
| `data/features/album_ids.pkl` | Master row index for all feature matrices (1,758,488) — committed |
| `data/features/album_genre_matrix.npz` | Combined album+artist+label tags (10,255 cols) — used in v3, weighted app |
| `data/features/album_record_label_matrix.npz` | Labels + types combined (2,968 cols) — used in v3, weighted app |
| `data/features/album_ratings_matrix.npz` | Bayesian-weighted album ratings |
| `data/features/album_country_matrix.npz` | One-hot artist country (2,014 cols) |
| `data/features/album_track_stats_matrix.npz` | 12 track-length statistics, min-max scaled |
| `data/features/album_era.parquet` | Per-album best_year, best_year_source, era_bin (audit use) |
| `data/features/album_era_matrix.npz` | One-hot era bin (10 cols: Pre-1900, 1900–1949, 1950s–2020s) — superseded by temporal matrix |
| `data/features/album_temporal_matrix.npz` | Era one-hot (10 cols) + continuous year col (1 col); loaded by app as the Era block |
| `data/features/temporal_year_scaler.json` | year_min / year_max for scaling the year column to [0,1] |
| `archive/album_tags_matrix.npz` | Album tags only (2,684 cols) — v1/v2; superseded by album_genre_matrix |
| `archive/album_labels_matrix.npz` | Record label identity (2,958 cols) — v1/v2; superseded by album_record_label_matrix |
| `archive/album_types_matrix.npz` | Label type (10 cols) — v1/v2; superseded by album_record_label_matrix |
| `data/model/knn_model.joblib` | v1 fitted model |
| `data/model_v2/knn_model_v2.joblib` | v2 fitted model |
| `data/model_v3/knn_model_v3.joblib` | v3 fitted model |

## Key design decisions

- **Album scope = `valid_albums`** (defined in `1-data/01-postgres-to-parquet.ipynb`): primary type = Album, must have an Official release (drops bootlegs), and is either studio (no secondary types), live (secondary type Live), or single-artist best-of (Compilation, non-VA). Other secondary types (Soundtrack, Remix, DJ-mix, Demo, …) and VA compilations excluded. Replaced the old "type=1 only" filter, which let in live bootlegs, remix collections and comps (e.g. U2 had 1,004 "albums" → 45 after the fix).
- **Bayesian weighted rating:** `score = (R × v) / (v + C)` with C=5. Collapses to 0 for unrated items.
- **Column pruning:** columns below `safe_threshold` nnz are dropped so no album with features loses all signal.
- **Same-artist exclusion:** recommendations filter out other albums by the same primary artist.
- **Album dropdown filtered** to albums present in any model's index.
- **Genre matrix blending (3-tier):** album tags always at w=1.0; **artist tags added for ALL albums** at w=0.5 (the old `< 5 direct tags` threshold was dropped — universal artist tags add *richness* to already-covered albums, not coverage); label tags at w=0.3 in two modes — **reinforcement** (masked to only boost tags the album/artist already has — ~87% of raw label-tag entries are masked out as genre-noise) and **rescue** (allowlist labels only, only for albums with zero album+artist signal). Rows L1-normalised after combining. Genre coverage: **1,210,648 albums (68.8%)** — near the tag-signal ceiling.
- **Label genre-coherence allowlist:** a label joins the allowlist if ≥60% of its albums share at least one tag with the label (per-album overlap rate). Label size and `label_type` are poor proxies for genre coherence (it is bimodal at every size), so the overlap rate is used directly. Only allowlist labels may *introduce* new genre signal for zero-signal albums. Computed at build time in `feature-genre.ipynb`.
- **Coverage vs richness:** coverage = does an album have any genre signal (binary); richness = how many distinct genre tags an already-covered album carries. Universal artist tags give **+0 coverage** (provably — the only newly-blended albums are ≥5-direct-tag albums that were already covered) but increase richness, which sharpens cosine similarity. See `2-eda/05-EDA-tags-labels.ipynb`.
- **Country downweighted to 0.2** — at w=1.0 it dominated cosine similarity due to density vs sparse tag columns.
- **Label weighting (fixed):** `album_label` parquet has one row per (album, label, tag) triple. Original weighting by `tag_count` underweighted boutique labels with few tags. Fixed to dedup (album, label) pairs and weight equally (1 / labels-per-album); the dedup slightly increases the label column count. (Current labels matrix: 2,958 cols at the post-rebuild universe.)
- **`album_record_label_matrix`** (v3) combines label identity + label type — both derived from `album_label`, encoding the same fact from two angles. v1/v2 keep them separate.
- **`album_types_matrix` is label types**, not release types — a side-product of the label feature build. Naming is misleading. It can NOT be used to detect live/compilation albums; the app's filters use the dedicated flag parquets instead (release-group secondary types from Postgres). See [[reference-mb-release-types]].
- **App album filters use exact MB secondary types, not name heuristics.** Live = secondary type 6, Compilation = 1. Flags pre-exported to single-column parquets and read into sets; an earlier album-name regex mis-classified titles like "Set List" and date-format concert names.
- **Label tags live only in the genre matrix.** The `tag_id` column in `album_label` feeds `album_genre_matrix` exclusively; it must NOT be used to weight the labels/record_label matrix (that was the bug above).
- **`artist_tags_matrix.npz`** no longer exists as a committed file — artist tags are blended into `album_genre_matrix` during the feature build. The EDA notebook (`05-EDA-tags-labels.ipynb`) rebuilds artist-tag statistics inline from `mb_artist_tag.parquet`.
- **Full-universe matrices:** all feature matrices are built and saved at the full album universe (currently 1,758,488) so block row dimensions always align for hstack. Mixing universes between blocks causes hstack dimension errors.

## Environment setup

```bash
source env/bin/activate
pip install -r requirements.txt
streamlit run 5-app/app_v3_weighted.py
```

Postgres credentials go in `.env` (see `.env.example`). Only needed to re-import (`1-data/01`)
and for the country/track-stats SQL (`1-data/03`, `1-data/04`).

## Current state (as of 2026-06-09)

- **Album scope fixed and full pipeline rebuilt.** Re-imported via the `valid_albums` scope;
  universe 2,241,402 → 1,758,488 release groups (U2: 1,004 → 45). All parquets, feature
  matrices, and the album/artist index were rebuilt and committed.
- **Current app: `app_v3_weighted.py`** — v3 feature blocks with runtime weighting, reading
  raw matrices (no trained model). Album dropdown filtered to albums queryable under current weights.
- **App UI reworked (`app_features` branch):** sliders replaced by custom HTML/SVG sidebar widgets —
  a 0–11 rotary-knob panel (`knob_component/index.html`) and two side-by-side vertical mixing-console
  faders (`knob_component/switch.html`) for the Live Albums and Greatest Hits filters, backed by
  MB-secondary-type flag parquets. Each widget owns its own value (no session_state mirror; knob
  reset via a nonce). Artist field is now a live `streamlit-searchbox` typeahead over the lookup.
  UI copy refreshed (header "Tune your sound", results caption "EQ —", Match % column). Two new SQL
  exports in `1-data/queries/`.
- Models v1/v2/v3 retrained at the new universe (v1 5,653/1.07M, v2 7,679/1.76M, v3 15,247/1.76M)
  but **gitignored** (large; the weighted app doesn't need them).
- Label-weight bug fixed earlier (dedup + equal weighting); `album_record_label_matrix` consolidates
  labels + types for v3.
- Feature diagnostic charts in `3-features/feature_charts/` (re-runnable via `generate_feature_charts.py`).
- **Era feature added.** `mb_release_year.parquet` (new import from `release_group_meta`) + era
  binning notebook (`3-features/07-feature-era.ipynb`) produce `album_era_matrix.npz` (12 cols, ~97.9%
  coverage). ERA knob added to `app_v3_weighted.py` (default dial 4). EDA in `2-eda/04-EDA-year.ipynb`.
  Five confirmed MusicBrainz year errors corrected (web-verified).
- **Tag/label notebooks restructured.** The old `01-feature-tags-labels.ipynb` and `10-feature-genre-tags.ipynb`
  were split into `3-features/01-album-artist-index.ipynb` (shared ID index), `3-features/02-feature-genre.ipynb`,
  and `3-features/03-feature-label.ipynb`; tag/label EDA in `2-eda/05-EDA-tags-labels.ipynb`.
- **Genre blend reworked.** Artist tags now applied to ALL albums (was `< 5 direct tags`); label tags
  split into masked reinforcement + allowlist rescue. New per-label genre-coherence allowlist (≥60%
  per-album tag overlap). Genre coverage 68.8% (1,210,648 albums) — near the tag-signal ceiling.
  Coverage-vs-richness analysis documented in the EDA notebook. `album_genre_matrix.npz` rebuilt
  (~5.45M nnz). **Note: v3 model not yet retrained against the rebuilt genre matrix.**
- **Temporal feature shipped.** `07-feature-era.ipynb` and `10-feature-year.ipynb` consolidated into `3-features/14-feature-temporal.ipynb`, which builds all temporal outputs in one top-to-bottom run: `album_era.parquet`, `album_era_matrix.npz`, `album_temporal_matrix.npz`, `temporal_year_scaler.json`. The app's Era block now loads `album_temporal_matrix.npz` (11 cols: 10 era one-hot + 1 continuous year). `YEAR_WEIGHT=0.3` set analytically — year range spans 975–2026 (1051 yrs), so the year column has no intra-decade resolution power; its only effect is era-boundary smoothing (~0.10 temporal cosine for boundary pairs). EDA analysis in `2-eda/04-EDA-year.ipynb`. Old notebooks and intermediate artifacts moved to `archive/`.
- **EDA tags-labels notebook refactored.** `2-eda/05-EDA-tags-labels.ipynb` no longer depends on any archived NPZ files — all per-source statistics derived from raw parquets. All four parquets loaded once in the setup cell; `DATA_DIR`, `MIN_TAG_OCC`, `W_LABEL`, `LABEL_OVERLAP_THRESHOLD` consolidated into `imports`. Genre vocab computed once. Label masking analysis fixed to use album + artist signal (matches production pipeline). Improvements documented in `Planning/feature-genre.md`.
- **Last.fm popularity feature added** (`lastfm` branch). Parallel scraper → `data/lastfm_data.parquet`
  (207k rows); feature notebook `3-features/13-feature-lastfm-popularity.ipynb` builds
  `album_lastfm_popularity_matrix.npz` (1.76M × 4, ~56% coverage). Sixth knob in app (Popularity,
  default dial 4); ratings weight auto-syncs to popularity dial. Scraper parked in `lastfm_merge/`
  pending placement into the 5-folder structure.

## Potential next steps

- Spotify API integration for playlist output
- Rating feedback loop
- Display era label in recommendation results table
- A "best of v4" config baked from the most useful slider weights discovered in the weighted app
- Use the Last.fm `weight` column (popularity) as a recommendation signal / tiebreaker
- Swap brute-force KNN for approximate NN (e.g. FAISS) if dataset grows
