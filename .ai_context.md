# AI Context — mixtape

Internal reference for Claude Code sessions. Updated as the project evolves.

## What this project is

Album recommendation engine trained on the MusicBrainz open music database. A user enters an artist name, selects an album, and receives 10 similar album recommendations via a KNN model. Three model versions exist and can be compared side-by-side in the app.

## Tech stack

- **Data store:** MusicBrainz PostgreSQL (source) → Parquet files (working copy) → DuckDB (query layer for feature extraction)
- **ML:** scikit-learn `NearestNeighbors` with cosine distance, sparse scipy matrices
- **App:** Streamlit
- **Environment:** Python 3.11, virtualenv in `env/`

## Project structure

```
mixtape/
├── 1-EDA/                              # Data import and exploration
│   ├── 01-postgres-to-parquet.ipynb    # Imports Parquet tables from Postgres via DuckDB
│   ├── 02-parquet-to-dataframes.ipynb  # Merges Parquet into pickled DataFrames
│   ├── 03-EDA-albums.ipynb
│   ├── 04-EDA-artists.ipynb
│   ├── 05-EDA-master.ipynb
│   ├── 06-schema-diagrams.md
│   └── SchemaSpy/
├── 2-Prototyping/                      # Feature engineering and model training
│   ├── 01-feature-tags-labels.ipynb    # Tag, label, type sparse matrices + master ID index
│   ├── 02-feature-ratings.ipynb        # Bayesian-weighted rating vectors
│   ├── 03-feature-assembly.ipynb       # Inspect combined matrix (display only)
│   ├── 04-knn-training.ipynb           # Trains v1 KNN, saves to data/model/
│   ├── 05-knn-query.ipynb              # KNN query prototyping (display only)
│   ├── 06-impute-artist-country.ipynb  # Builds artist_country_fast via DuckDB SQL (needs Postgres)
│   ├── 07-build-album-track-stats.ipynb # Builds album_stats via DuckDB SQL (needs Postgres)
│   ├── 08-feature-country.ipynb        # Builds album_country_matrix.npz
│   ├── 09-feature-track-stats.ipynb    # Builds album_track_stats_matrix.npz
│   ├── 10-feature-genre-tags.ipynb     # Builds album_genre_matrix.npz (album+artist+label tags)
│   ├── 11-knn-v2-training.ipynb        # Trains v2 KNN, saves to data/model_v2/
│   ├── 12-knn-v3-training.ipynb        # Trains v3 KNN, saves to data/model_v3/
│   ├── feature_charts/                 # Saved feature diagnostic plots + generate_feature_charts.py
│   └── queries/                        # SQL files for DuckDB feature extraction
│       ├── mb_artist_country_fast_duckdb_release.sql
│       └── mb_album_stats_duckdb.sql
├── 3-app/
│   ├── app.py                          # Streamlit app — v1 only
│   ├── app_v2.py                       # Streamlit app — v1 vs v2 side-by-side
│   └── app_v3.py                       # Streamlit app — v1 vs v2 vs v3 side-by-side
├── data/
│   ├── mb_*.parquet                    # MusicBrainz exports
│   ├── sql_feature_*.parquet           # Feature-specific exports (country, track stats)
│   ├── pickles/                        # Intermediate DataFrames
│   ├── features/                       # Sparse feature matrices (see below)
│   ├── model/                          # v1 model artefacts
│   ├── model_v2/                       # v2 model artefacts
│   └── model_v3/                       # v3 model artefacts
├── docs/
├── musicbrainz.duckdb
├── requirements.txt
└── README.md
```

## Model versions

| Version | Feature blocks | Tag columns | Features (post-prune) | Albums indexed | Notebook |
|---------|---------------|-------------|----------------------|----------------|----------|
| v1 | tags · labels · types · ratings | 3,041 | 6,612 | 1,352,482 | 04-knn-training.ipynb |
| v2 | v1 + country · track stats | 3,041 | 8,887 | 2,240,303 | 11-knn-v2-training.ipynb |
| v3 | genre tags · record_label · ratings · country · track stats | 11,247 | 17,090 | 2,240,363 | 12-knn-v3-training.ipynb |

v3 is the current best model. `app_v3.py` is the active app.

Note: v1/v2 use the separate `album_labels_matrix` (3,560 cols) + `album_types_matrix` (10 cols); v3 uses the combined `album_record_label_matrix` (3,570 cols). All matrices are now built and saved at the full album universe (2,241,402 rows), so v2/v3 index ~2.24M albums vs v1's 1.35M (v1's tag-only feature set leaves more albums with no signal).

## Pipeline stages (run in order)

### 1-EDA
1. `01-postgres-to-parquet.ipynb` — connects to MusicBrainz Postgres via DuckDB, exports tables to parquet
2. `02-parquet-to-dataframes.ipynb` — joins parquets into pickled DataFrames
3. `03–05` — EDA reports (display only)

### 2-Prototyping (v1)
4. `01-feature-tags-labels.ipynb` — builds tag, label, type sparse matrices; creates master album/artist ID index
5. `02-feature-ratings.ipynb` — computes Bayesian-weighted rating vectors
6. `03-feature-assembly.ipynb` — inspect and validate (display only)
7. `04-knn-training.ipynb` — assembles, prunes, normalises, trains, saves v1

### 2-Prototyping (v2/v3 — steps 6–7 need Postgres)
8. `06-impute-artist-country.ipynb` — DuckDB SQL → `sql_feature_artist_country_fast.parquet`
9. `07-build-album-track-stats.ipynb` — DuckDB SQL → `sql_feature_album_track_stats.parquet`
10. `08-feature-country.ipynb` — `album_country_matrix.npz`
11. `09-feature-track-stats.ipynb` — `album_track_stats_matrix.npz`
12. `10-feature-genre-tags.ipynb` — `album_genre_matrix.npz`
13. `11-knn-v2-training.ipynb` — trains v2
14. `12-knn-v3-training.ipynb` — trains v3

## Key data files

| Path | Description |
|------|-------------|
| `data/mb_*.parquet` | Raw MusicBrainz exports |
| `data/sql_feature_artist_country_fast.parquet` | Artist country with 10-signal imputation (2.86M artists) |
| `data/sql_feature_album_track_stats.parquet` | Album track statistics (2.24M albums) |
| `data/features/album_ids.pkl` | Master row index for all feature matrices |
| `data/features/album_tags_matrix.npz` | Album tags only (3,041 cols) — used in v1/v2 |
| `data/features/album_genre_matrix.npz` | Combined album+artist+label tags (11,247 cols) — used in v3 |
| `data/features/album_labels_matrix.npz` | Record label identity (3,560 cols) — used in v1/v2 |
| `data/features/album_types_matrix.npz` | Label type (10 cols, not release type) — used in v1/v2 |
| `data/features/album_record_label_matrix.npz` | Labels + types combined (3,570 cols) — used in v3 |
| `data/features/album_ratings_matrix.npz` | Bayesian-weighted album ratings |
| `data/features/album_country_matrix.npz` | One-hot artist country (2,263 cols) |
| `data/features/album_track_stats_matrix.npz` | 12 track-length statistics, min-max scaled |
| `data/model/knn_model.joblib` | v1 fitted model |
| `data/model_v2/knn_model_v2.joblib` | v2 fitted model |
| `data/model_v3/knn_model_v3.joblib` | v3 fitted model |

## Key design decisions

- **Only `release_group` type=1** (albums) — no singles/EPs.
- **Bayesian weighted rating:** `score = (R × v) / (v + C)` with C=5. Collapses to 0 for unrated items.
- **Column pruning:** columns below `safe_threshold` nnz are dropped so no album with features loses all signal.
- **Same-artist exclusion:** recommendations filter out other albums by the same primary artist.
- **Album dropdown filtered** to albums present in any model's index.
- **Genre matrix blending:** artist tags only added for albums with < 5 direct tags (w=0.5, 92.2% of albums qualify); label tags always added (w=0.3); album tags always at w=1.0. Rows L1-normalised after combining.
- **Country downweighted to 0.2** — at w=1.0 it dominated cosine similarity due to density vs sparse tag columns.
- **Label weighting (fixed):** `album_label` parquet has one row per (album, label, tag) triple. Original weighting by `tag_count` underweighted boutique labels with few tags. Fixed to dedup (album, label) pairs and weight equally (1 / labels-per-album). Labels matrix grew 3,469 → 3,560 cols.
- **`album_record_label_matrix`** (v3) combines label identity + label type — both derived from `album_label`, encoding the same fact from two angles. v1/v2 keep them separate.
- **`album_types_matrix` is label types**, not release types — a side-product of the label feature build. Naming is misleading.
- **Label tags live only in the genre matrix.** The `tag_id` column in `album_label` feeds `album_genre_matrix` exclusively; it must NOT be used to weight the labels/record_label matrix (that was the bug above).
- **`artist_tags_matrix.npz`** exists but is not used directly in any model — artist tags are blended into `album_genre_matrix` instead.
- **Full-universe matrices:** all feature matrices are built and saved at the full 2,241,402-album universe so block row dimensions always align for hstack. Earlier mixing of the 1M annotated subset with full-universe blocks caused hstack dimension errors.

## Environment setup

```bash
source env/bin/activate
pip install -r requirements.txt
streamlit run 3-app/app_v3.py
```

Postgres credentials go in `.env` (see `.env.example`). Only needed for notebooks 06 and 07.

## Current state (as of 2026-06-03)

- Three model versions operational: v1, v2, v3. All retrained against the full-universe, label-fix matrices.
- v3 is the current best: combines album+artist+label tags into a unified genre matrix (11,247 tags vs 3,041 in v1/v2) and uses the consolidated `album_record_label_matrix`.
- Label-weight bug fixed (dedup + equal weighting); labels matrix 3,469 → 3,560 cols.
- All feature matrices rebuilt at the full 2,241,402-album universe; v2/v3 now index ~2.24M albums.
- `app_v3.py` shows all three models side-by-side with per-model unique recommendation highlighting.
- Feature diagnostic charts collected in `2-Prototyping/feature_charts/` (re-runnable via `generate_feature_charts.py`).
- Project restructured into `1-EDA/`, `2-Prototyping/`, `3-app/` sections.
- Active branch: `tag_feature` (branched from `sql_features`, merged `cleanup` folder structure).

## Potential next steps

- Spotify API integration for playlist output
- Rating feedback loop
- Filters: year, genre, country
- Tune country and genre tag weights via the app UI (runtime reweighting from raw feature matrices)
- Swap brute-force KNN for approximate NN (e.g. FAISS) if dataset grows
